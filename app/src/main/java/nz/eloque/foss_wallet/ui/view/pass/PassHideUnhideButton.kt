package nz.eloque.foss_wallet.ui.view.pass

import nz.eloque.foss_wallet.R

@Composable
fun PassHideUnhideButton(
    IconButton(onClick = {}) {
        Icon(imageVector = Icons.Default.Share, contentDescription = stringResource(R.string.share))
    }
}
